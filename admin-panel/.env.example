# Admin API origin
VITE_API_BASE_URL=http://localhost:5000/api/v1

# CDN origin serving uploaded assets. Unset, asset paths resolve against
# the API origin, which is what makes local development work without a
# CDN. Set this in every deployed environment.
VITE_ASSET_BASE_URL=https://static.timesauto.net
